const tempServer = require("../WebServer");
const assert = require( "assert" );

describe( "Server capacity", function () {
  
});

describe( "Web Page", function () {
  
});

describe( "Login", function () {
  
});

describe( "Visitor", function () {
  
});
